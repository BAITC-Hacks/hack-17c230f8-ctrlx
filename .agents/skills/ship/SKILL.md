---
name: ship
description: Подготовка к сдаче — README по п. 5.4.15 и инструкции организаторов с трассировкой требований, smoke-тест со свежего clone, независимая проверка Codex-судьёй, список фиксов. Запускать вручную после фриза (16:30) и перед финальным push.
disable-model-invocation: true
---

# Ship: сдача, которую нельзя завалить

Эксперт или AI-судья клонирует репо и делает шаги из README. Не запустилось — команда выбывает, исправления не принимаются (п. 5.4.16). Оценивают и документацию, и структуру, и тесты.

1. Сверь `README.md` с реальностью: код, скрипты `package.json`/`pyproject.toml`, `.env.example`. README — на русском. Разделы шаблона идут в порядке из инструкции организаторов — не переставляй и не переименовывай: название; краткое описание (какую проблему решает и для кого); что реализовано; как работает решение (сценарий от входных данных до результата); технологии (языки, фреймворки, библиотеки, AI-модели, API, внешние сервисы); архитектура (mermaid); установка и запуск (+ зависимости и таблица переменных окружения по 5.4.15); как проверить решение (копируемые команды: вход → ожидаемый выход); данные и интеграции; ограничения; ссылка на развёрнутую версию. Пиши только то, что подтверждается репозиторием: не придумывай функции, технологии и результаты. Заполни все разделы и удали все подсказки `<!-- -->`. Замени `title`/`description` в `app/layout.tsx` (web) или `<title>` в `static/index.html` (py) на название продукта.
2. **Трассировка**: таблица R-ID → фича → файл/эндпоинт → как проверить → статус (✅ / ⚠️ / ❌). Не сделано — так и пиши.
3. **Заранее подготовленное и AI-инструменты**: ссылка на `docs/PREPARED.md`, заимствования с лицензиями, какие AI-инструменты использовались.
4. Быстрый старт — не больше 3 команд, работает без ключей. Live URL — вставь, если есть. Проверка без личных аккаунтов (п. 5.6.6): вход в приложении → логин и пароль демо-аккаунта в «Как проверить решение»; ключи API/БД и пароли от панелей сервисов в README не пишем никогда — на проде ключи на сервере (`vercel env`), локально DEMO.
5. Обнови в `scripts/smoke.sh` значения `SMOKE_PATH` / `SMOKE_BODY` под основной сценарий.
6. `scripts/checkpoint.sh "docs: README for submission"` → `scripts/smoke.sh` (проверяет то, что на GitHub). Любой FAIL чинится первым.
7. `scripts/judge.sh` (Codex на свежем clone, 3–6 мин, можно в фоне) → сделай из его топ-5 фиксов те, что влезают до 17:40.
8. **`docs/TESTING.md`** — журнал проверок по шаблону: автотесты (что покрывают, R-ID, результат), итог smoke, ручные сценарии (DEMO и LLM, 1280×800 и 375 px), оценка judge и что исправили, известные ограничения. Только то, что реально выполнено. Сошлись на него из README («Тесты и метрики»).
9. **Проверь каждое утверждение** README и `docs/TESTING.md` — команды, пути, цифры, статусы R-ID, ссылки: выполни или открой — и выведи в чат таблицу «утверждение → как проверено → ✅/❌». Каждое ❌ исправь или убери.
10. Итог в чат — чек-лист сдачи: README, TESTING, smoke, оценка судьи, live URL; и что осталось. Отдельным блоком — текст для формы «Сдать решение» на платформе: название (≤ 60 символов) и описание (2–3 предложения: проблема и для кого, что сделано, как проверить), ссылка на репозиторий.
