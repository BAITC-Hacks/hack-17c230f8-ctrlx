# Метрики и происхождение результатов (R1, R3, R7)

Результаты ниже получены 23.09.2026 после исправления цели станции и разделения
обучения/калибровки CQR. Источники чисел —
`outputs/metrics/holdout_2026-01.json` и `holdout_2025-02.json`.
Команды запущены на версии `4a37810`; параметры GBM не менялись по результатам
этой проверки. Последующие исправления оркестратора не меняют эти метрики модели.

## Воспроизведение

```bash
uv sync
uv run python -m app.cli train
uv run python -m app.cli evaluate --holdout 2026-01
uv run python -m app.cli evaluate --holdout 2025-02
```

При сохранённом кэше команды не требуют сети, LLM или ключей. `train` заменяет
production-модель; `evaluate` обучает отдельную модель строго до проверяемого
месяца и заменяет соответствующий JSON, не production pickle.

| Команда | Измеренное время wall clock |
|---|---:|
| `train` | 26,67 с |
| `evaluate --holdout 2026-01` | 25,40 с |
| `evaluate --holdout 2025-02` | 28,34 с |

Время измерено `/usr/bin/time -p` на рабочем Mac команды. Это один запуск каждой
команды; скорость чистой установки и другого компьютера отдельно не измерялась.

## Что именно оценивается

Цель станции — среднее наблюдений двух равномощных турбин, нормализованное к 1.
Если неизвестна хотя бы одна турбина, метка станции отсутствует; известная
индивидуальная метка остаётся для модели этой турбины. Медиана станции обучается
отдельно, поэтому не обязана совпадать со средним прогнозных медиан турбин.

- `MAE = mean(abs(prediction − observation))` — доля номинала.
- `nMAE = 100 × MAE` — процент номинала, не процент фактической выработки.
- `skill = 1 − MAE_model / MAE_baseline` — относительное снижение ошибки.
- `coverage` — доля оценённых пар, где факт внутри p10–p90; `mean_width` — средняя
  ширина этого интервала станции, в долях номинала.
- `24h` — опережение 0–23 ч; `48h` — 24–47 ч. `all` объединяет оба диапазона.
  Один целевой час может встречаться в двух выпусках, поэтому число пар и число
  уникальных часов различаются.

| Период | Выпуски | Пары D+1 | Пары D+2 | Пары всего | Уникальные часы |
|---|---:|---:|---:|---:|---:|
| Январь 2026 | 31 | 744 | 720 | 1 464 | 744 |
| Февраль 2025 | 28 | 666 | 642 | 1 308 | 666 |

В феврале 2025 нет наблюдений обеих турбин за 28.02, 06:00–11:00 UTC
(11:00–16:00 Алматы). Эти шесть часов исключены, не заполнены вымышленным фактом.

## Результаты первоначального выпуска

| Модель | Январь MAE | Январь nMAE | Февраль MAE | Февраль nMAE |
|---|---:|---:|---:|---:|
| Персистентность | 0,3066 | 30,66 % | 0,3386 | 33,86 % |
| Климатология | 0,3039 | 30,39 % | 0,3205 | 32,05 % |
| Кривая мощности | 0,1953 | 19,53 % | 0,2070 | 20,70 % |
| **GBM** | **0,1555** | **15,55 %** | **0,1771** | **17,71 %** |

| GBM | Январь | Февраль |
|---|---:|---:|
| MAE D+1 | 0,1509 | 0,1785 |
| MAE D+2 | 0,1602 | 0,1756 |
| RMSE всего | 0,2306 | 0,2455 |
| Bias всего | +0,0058 | +0,0192 |
| Skill к персистентности | 49,3 % | 47,7 % |
| Skill к кривой мощности | 20,4 % | 14,4 % |
| Покрытие p10–p90 | **81,6 %** | **82,0 %** |
| Средняя ширина p10–p90 | 0,501 | 0,590 |
| CQR qhat | 0,0103 | 0,0240 |

Это валидационные периоды: раньше по ним принимались проектные решения.
Они не являются полностью независимым тестом выбора архитектуры. Февраль 2026
не оценён по точности, поскольку факта в выданных данных нет. Улучшение числовой
точности от LLM не измерялось. `1 − MAE` — дополнительное преобразование ошибки,
а не доля правильно угаданных часов.

## Временные границы и CQR

Все границы ниже в UTC; `train_end` — исключающая граница. В JSON поле
`train_end` хранит только её календарную дату UTC.

| Назначение модели | Train end | Последняя цель fit CQR | Первая цель calibration |
|---|---|---|---|
| Февраль 2025 | 31.01.2025 19:00 | 02.12.2024 18:00 | 02.12.2024 19:00 |
| Январь 2026 | 31.12.2025 19:00 | 01.11.2025 18:00 | 01.11.2025 19:00 |
| Production | 31.01.2026 19:00 | 02.12.2025 18:00 | 02.12.2025 19:00 |

| Назначение | Метки станции | Fit CQR | Calibration CQR |
|---|---:|---:|---:|
| Февраль 2025 | 13 698 | 10 822 | 2 852 |
| Январь 2026 | 29 398 | 26 530 | 2 844 |
| Production | 30 886 | 28 018 | 2 844 |

По 24 пары вокруг границы исключаются из CQR: их выпуск раньше calibration,
а целевой час уже в calibration. Кривые мощности для квантильных признаков
также обучаются только до calibration. Точечная модель использует всю историю
до `train_end`. Наблюдения временного ряда коррелированы; универсальная
статистическая гарантия покрытия 80 % не заявляется.

Production pickle содержит 657 выпусков, `quantile_curves` для трёх полей,
`cqr_qhat = 0.01853008845530557` и метаданные границ. Размер — **4 530 808 байт**.
Время создания модели — `2026-09-23T11:37:16+00:00`; JSON оценок созданы
в `11:37:48.428259Z` и `11:38:22.813248Z` соответственно.

SHA-256 на момент этой проверки:

| Артефакт | SHA-256 |
|---|---|
| `models/windcast.pkl` | `5b0f36567da98a78191b4cbe8d45850aadc1aa5563d0de1ade2efd2a0fc814f2` |
| `outputs/metrics/holdout_2026-01.json` | `c63804572a1d88551107fea5550f265cf20fe3d09d889000082d2378c870485e` |
| `outputs/metrics/holdout_2025-02.json` | `a47138db70d9fa3e7fef8fedc00999b9dae99bc16fcf30fbf1dee3436ea06faf` |

Повторный запуск обновляет временные метки, поэтому побайтное совпадение pickle
и JSON не требуется. Проверяются процедура, границы и числовые результаты.

## Стоимость и вклад оркестратора

Сценарная стоимость умножает все абсолютные отклонения заявки D+2 на мощность
5 МВт, цену 22,68 тг/кВт·ч и коэффициент 0,3. Освобождение внутри ±5 % и
взаимозачёт не моделируются; режим фактического договора станции неизвестен.
Это условная верхняя оценка по **720 январским и 642 февральским часам**,
не счёт и не измеренная экономия. Первые сутки месяца в D+2 не покрыты.
GBM: 3,92/3,84 млн тг; кривая: 5,10/4,48; персистентность: 7,44/7,81.

После обновления модели `replay --month 2026-01` и `faults` выполнены повторно.
Источник — `outputs/evidence/replay_2026-01.json`: 31 выпуск, 1 464 пары,
обучение до `2025-12-31T19:00:00+00:00`.

| Вариант | MAE |
|---|---:|
| A: персистентность | 0,3066 |
| B: фиксированная кривая мощности | 0,1953 |
| C: GBM без пересчёта | 0,1555 |
| D: опубликованный план | **0,1536** |

В D первые сутки обновляются в 12:00, а заявка D+2 остаётся из выпуска t0.

| Сравнение | Выпуски | Средняя ΔMAE | 95 % интервал | Улучшений |
|---|---:|---:|---|---:|
| Проектный выбор GBM против кривой | 31 | −0,0407 | [−0,0563; −0,0255] | 24 |
| Пересчёт по свежей погоде | 31 | −0,0064 | [−0,0152; +0,0017] | 18 |

ΔMAE для пересчёта считается на затронутых часах; затем эффекты усредняются
по выпускам. Это не разность агрегированных MAE всей таблицы. Интервал пересчёта
включает ноль: устойчивое улучшение на новых периодах не доказано. Bootstrap
использует выпуски как блоки, но соседние выпуски разделяют целевые часы;
выводы по одному месяцу ограничены. В обычном январе запасная модель,
смена источника и флаг дрейфа не сработали.

Источник `outputs/evidence/faults.json` — выпуск 09.02.2026:

| Сбой | Решение агента | Валидные часы агента | Фиксированный конвейер |
|---|---|---:|---|
| Нет 6 часов ветра | GFS | 48 | 42 часа |
| Ветер 100 м/с | GFS | 48 | 48 часов, 6 по испорченному входу |
| 100 м/с только в свежем поле | Более старое поле | 48 | 48 часов, 6 по испорченному входу |
| Основной источник недоступен | GFS | 48 | 0 часов |
| Оба источника сломаны | Климатология | 48 | 42 часа |

Это **5 из 5 успешных проверок полноты**, не доказательство точности при сбое.
Февральский backtest также повторно сформирован: 28 выпусков и 672 строки
сшитого прогноза. Последний полный локальный прогон тестов: **155 passed,
8,09 с**; подробности итоговой проверки — в `TESTING.md`.

LLM-супервизор проверен локальными тестами, но настоящий API-прогон пользователь
отложил. Локально установлен `DEMO_MODE=1`; успешный внешний LLM-цикл не заявляется.
Будущее подтверждение — `supervisor.json` с `mode=llm`; порядок и ограничения —
[AGENTIC.md](AGENTIC.md).
