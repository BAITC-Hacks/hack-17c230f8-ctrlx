# amirkhan — задачи

<!-- Меняет только amirkhan (или его агент). Остальные читают: scripts/sync.sh показывает «Сейчас» и «Нужно от других» у всех. -->

Роль: **идеи и новые функции** + модуль погоды. Идеи — в `docs/IDEAS.md` (таблица: идея · ценность для диспетчера/жюри 1–5 · время · что нужно от других · статус), лиду сообщением `[IDEA]`; делать — только после ОК лида и только в своей зоне. Код: `app/weather.py` — изолированный модуль с тестом, чтобы у тебя были свои коммиты (организаторы смотрят коммиты каждого). Пуш через `scripts/checkpoint.sh` каждые ≤ 30 мин.

## Сейчас
- 17:27 — финальное состояние: код зоны (weather/ask + тесты) заморожен с 59b7db8; Windows-переносимость подтверждена на e3e7fb8; отвечаю только на `[REVIEW]` до финального push лида 17:40.

## Дальше
- Кандидаты идей: что усилит решение по критериям ТЗ (ценность 15, оригинальность 10, техреализация 25). Стартовые кандидаты: разброс best_match vs gfs как мера неопределённости и предупреждение диспетчеру; поправка на обледенение/плотность воздуха по температуре; детекция простоя турбины в истории и флаг «турбина 2 в ремонте» для прогноза; сводка для диспетчера по-русски (шаблон → LLM); экспорт прогноза в формат заявки на рынок (почасовая таблица МВт при заданной установленной мощности); оценка экономии на небалансе. Каждую — с оценкой и что нужно от других.
- R10 (после ОК лида) — `gfs_seamless` в кэше и в `load_or_fetch`; функция `spread(wx_best, wx_gfs)` → колонка разброса, которую агент использует как признак неопределённости.
- Согласованная идея — реализация в своей зоне или как раздел README/`docs/`.
- После 16:30 — чистый clone на Windows (Git Bash) по README, `/critics` по README и странице, замечания Ансару и лиду.

## Нужно от других
Аудит на 0846eeb, повторная сверка с 8c425b3 (17:06). Только ещё открытые замечания; чужие зоны не менялись.
- ansar (R8): static/index.html:570–585 — гонка loadIssue: старый HTTP-ответ может отрисовать предыдущую дату под новым селектором. Нужен request token после обоих await и в catch; воспроизведено с переставленными ответами.
- mustafa (R5/R10): orchestrator.py:427–429 — исключение fetch_weather по любому источнику обходит fallback. Проверить отдельно missing/corrupt primary и GFS, а не только пустой DataFrame в faults.
- mustafa (R7): evaluate.py:74–86 — holdout без факта (2026-02) даёт пустой ev и NaN-метрики; остановиться до обучения/сериализации с ясным сообщением.
- ansar (R6/R7): README сверить с 0d36840: Jan GBM nMAE 15.55%, coverage 81.6%; Feb-2025 17.71%, 82.0%; replay B/C/D .1953/.1555/.1536. Старые 84% и экономика расходятся с новыми JSON.
- ansar (R6): scripts/smoke.sh — фиксированный порт 8199 может проверить чужой сервер; cleanup убивает все PID порта. Проверять свой PID и останавливать только собственный процесс. Лид уже отметил это в 16:49.
- Старые замечания о DATA_TZ/PUBLISH_DELAY_H в REQUIREMENTS и E501 сняты: в актуальном коде исправлены.

## Готово
- 17:23 — **Windows verified на e3e7fb8** (portable-модель, feature_decimals=12): чистый clone (Git Bash, Python 3.12.14, sklearn 1.9.1, numpy 2.5.3, pandas 3.0.6), `uv sync` 2.8 с, `backtest` 29.6 с → 29/29 CSV byte-identical с GitHub, `pytest` 203 pass, `ruff` чист.
- 17:01–17:16 — найден и локализован дефект переносимости: на 0846eeb Windows-регенерация расходилась с macOS в 338/2296 строк (max |Δ| power_t1 0.090, farm 0.048) при идентичных погоде, run_id и pkl; Win→Win повтор идентичен. Пример для лида: issue 2026-02-20, 22.02 14:00, lead 38 — power_t1 0.6265 (mac) / 0.7166 (win). Лид воспроизвёл причину (+1 ULP в `dir_cos` → ветвление деревьев) и исправил канонизацией признаков до 12 знаков + переобучением (e3e7fb8).
- 16:51 0846eeb — hardening по аудиту: `run_id` валидируется (нет выхода из `runs/`), имя погодной модели валидируется до путей/URL, HTML из ответа LLM вычищается, «1 000»/«00» нормализуются, `sources` LLM сверяются с фактами, `grounded` считается и для шаблона, прогон без CSV отвечает «строк прогноза нет». Проверки только на чтение: правило утечки на всех строках 28 выпусков (rev0/rev1), инварианты CSV/логов, все GET API и traversal → 404, секретов нет.
- 17:09 — дополнительный аудит R2/R9: malformed JSON/пустой список точек/неверные даты/дубликаты/неравные массивы/nonfinite значения отвергаются до замены погодного кэша; partial live-window не признаётся совпадением; SHA256 проверяется по байтам. Без CSV не используются ни старый report, ни LLM; несуществующие report/revision и полностью выдуманные ссылки не принимаются. MW/MWh явно помечены как сценарный номинал, время публикации — как допущение политики. Добавлены 22 регрессионные проверки поверх 0846eeb: ruff чист, 188 passed при PYTHONUTF8=1.
- 17:05 — офлайн приёмка текущего Python-ядра с выводом в отдельный work/release-check каталог: два backtest за 49.95 с, 28 выпусков × 48 часов rev0, 672 уникальных часа февраля, issue CSV побайтно повторяются, 32 GET и POST /api/run через TestClient прошли. Сеть заблокирована для проверки; это не браузерный smoke новой Next.js-платформы.
- Windows: без PYTHONUTF8=1 один новый тест блокировки падает на сравнении кириллицы stderr subprocess; с PYTHONUTF8=1 весь набор проходит. Логику межпроцессной блокировки проверили отдельно.
- 17:08 — замечание о перезаписи месячного CSV закрыто лидом в c7206b1: partial range отдельно, проверка диапазона и атомарный writer; не числится открытым.
- 16:20 — чистый clone на Windows (Git Bash) по README, HEAD fe4ee94: `uv sync` 2.5 с, `backtest` 64 с → 28 `issue_*.csv` + `february_2026.csv`, `pytest` 110 pass, `ruff` чист, `scripts/smoke.sh` → SMOKE PASS. Замечаний нет.
- ~16:15 c7363b1 — R9 `app/ask.py`: `answer(run_id, question) -> AskAnswer` — факты из `RunLog.read`, строк issue CSV этого run_id (все ревизии) и `report.md`; шаблонные ответы (пик, штиль, энергия по суткам, конкретный час, коридор, погода/утечка, пересчёт, модель, шаги); с ключом — `llm.complete_json`, проверка каждого числа ответа по множеству чисел фактов, иначе шаблон с `fallback_reason`. `tests/test_ask.py` — 12 тестов, в т. ч. «шаблонный ответ сам проходит проверку чисел». Для Ансара: `from app.ask import answer`.
- ~16:05 c18fe8e — R2/P0 (SOLUTION 14): `app/weather.py` запрос по координатам обеих турбин (`turbine_points`, ячейки сетки и `same_cell` в `meta.json`), `to_frame` принимает список точек (среднее по точкам, направление — круговое среднее), `fetch_issue_window(t0, model)` — живая сверка окна выпуска с кэшем (`live`/`mismatches`/`max_abs_diff`), офлайн → `live=False` без исключений; кэш обеих моделей перекачан с двумя точками; тесты.
- 15:59 6485d25 — по [REVIEW] лида: тесты утечки по значениям (синтетика `ws100_dN = 100N + lead`, 28 выпусков × hsi 0/12, day2-поля только назад, откат только на старший прогон), понятные `FileNotFoundError`/`ValueError` вместо пустого кадра, атомарная запись кэша, проверка ответа до записи, `attrs["source"]`. 91 тест зелёный.
- R2/R7/R10 — подготовлен research в `docs/IDEAS.md`: независимый аудит CSV, единицы и сценарная экономика, проверка метаданных одного NOAA GFS run, мировые первичные источники, stakeholder/trend/pitch рекомендации. Это исследование, не новая реализация или пересчитанные метрики. Выявленные риски кэша/документации записаны отдельно; проверка: `uv run pytest -q` — 25 passed; существующий E501 в `app/models.py:88` записан выше. Передача через командный checkpoint.
- R2 — `app/weather.py` (fetch_previous_runs, load_or_fetch, select_for_issue) + `tests/test_weather.py` (11 тестов: N ≥ safe_previous_day, 48 строк без NaN для первого и последнего выпуска, офлайн с кэшем и без). Кэш `best_match` и `gfs_seamless` до 2026-03-02 (end_date сдвинут по [IDEA] Ансара: последний выпуск покрывает 01.03), `meta.json` с url/fetched_at/sha256 по моделям. Поля только day2 (dir/temp/gust) при lead ≥ 42 протягиваются с последнего безопасного часа.
