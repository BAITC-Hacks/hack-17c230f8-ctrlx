# amirkhan — задачи

<!-- Меняет только amirkhan (или его агент). Остальные читают: scripts/sync.sh показывает «Сейчас» и «Нужно от других» у всех. -->

Роль: **идеи и новые функции** + модуль погоды. Идеи — в `docs/IDEAS.md` (таблица: идея · ценность для диспетчера/жюри 1–5 · время · что нужно от других · статус), лиду сообщением `[IDEA]`; делать — только после ОК лида и только в своей зоне. Код: `app/weather.py` — изолированный модуль с тестом, чтобы у тебя были свои коммиты (организаторы смотрят коммиты каждого). Пуш через `scripts/checkpoint.sh` каждые ≤ 30 мин.

## Сейчас
- Фриз: только проверка. Отвечаю на `[REVIEW]` по `app/ask.py` и `app/weather.py`, при необходимости повторяю чистый clone после финальных правок README.

## Дальше
- Кандидаты идей: что усилит решение по критериям ТЗ (ценность 15, оригинальность 10, техреализация 25). Стартовые кандидаты: разброс best_match vs gfs как мера неопределённости и предупреждение диспетчеру; поправка на обледенение/плотность воздуха по температуре; детекция простоя турбины в истории и флаг «турбина 2 в ремонте» для прогноза; сводка для диспетчера по-русски (шаблон → LLM); экспорт прогноза в формат заявки на рынок (почасовая таблица МВт при заданной установленной мощности); оценка экономии на небалансе. Каждую — с оценкой и что нужно от других.
- R10 (после ОК лида) — `gfs_seamless` в кэше и в `load_or_fetch`; функция `spread(wx_best, wx_gfs)` → колонка разброса, которую агент использует как признак неопределённости.
- Согласованная идея — реализация в своей зоне или как раздел README/`docs/`.
- После 16:30 — чистый clone на Windows (Git Bash) по README, `/critics` по README и странице, замечания Ансару и лиду.

## Нужно от других
- mustafa/ansar (R2/R6): синхронизировать README/REQUIREMENTS с текущими PUBLISH_DELAY_H=7 и DATA_TZ; подробности в `docs/IDEAS.md`, «Read-only review перед checkpoint». Контракт самостоятельно не менялся.
- mustafa (R6): базовый `ruff check .` сообщает E501 в `app/models.py:88` (103 > 100). Файл вне нашей зоны не изменялся.

## Готово
- 16:20 — чистый clone на Windows (Git Bash) по README, HEAD fe4ee94: `uv sync` 2.5 с, `backtest` 64 с → 28 `issue_*.csv` + `february_2026.csv`, `pytest` 110 pass, `ruff` чист, `scripts/smoke.sh` → SMOKE PASS. Замечаний нет.
- ~16:15 c7363b1 — R9 `app/ask.py`: `answer(run_id, question) -> AskAnswer` — факты из `RunLog.read`, строк issue CSV этого run_id (все ревизии) и `report.md`; шаблонные ответы (пик, штиль, энергия по суткам, конкретный час, коридор, погода/утечка, пересчёт, модель, шаги); с ключом — `llm.complete_json`, проверка каждого числа ответа по множеству чисел фактов, иначе шаблон с `fallback_reason`. `tests/test_ask.py` — 12 тестов, в т. ч. «шаблонный ответ сам проходит проверку чисел». Для Ансара: `from app.ask import answer`.
- ~16:05 c18fe8e — R2/P0 (SOLUTION 14): `app/weather.py` запрос по координатам обеих турбин (`turbine_points`, ячейки сетки и `same_cell` в `meta.json`), `to_frame` принимает список точек (среднее по точкам, направление — круговое среднее), `fetch_issue_window(t0, model)` — живая сверка окна выпуска с кэшем (`live`/`mismatches`/`max_abs_diff`), офлайн → `live=False` без исключений; кэш обеих моделей перекачан с двумя точками; тесты.
- 15:59 6485d25 — по [REVIEW] лида: тесты утечки по значениям (синтетика `ws100_dN = 100N + lead`, 28 выпусков × hsi 0/12, day2-поля только назад, откат только на старший прогон), понятные `FileNotFoundError`/`ValueError` вместо пустого кадра, атомарная запись кэша, проверка ответа до записи, `attrs["source"]`. 91 тест зелёный.
- R2/R7/R10 — подготовлен research в `docs/IDEAS.md`: независимый аудит CSV, единицы и сценарная экономика, проверка метаданных одного NOAA GFS run, мировые первичные источники, stakeholder/trend/pitch рекомендации. Это исследование, не новая реализация или пересчитанные метрики. Выявленные риски кэша/документации записаны отдельно; проверка: `uv run pytest -q` — 25 passed; существующий E501 в `app/models.py:88` записан выше. Передача через командный checkpoint.
- R2 — `app/weather.py` (fetch_previous_runs, load_or_fetch, select_for_issue) + `tests/test_weather.py` (11 тестов: N ≥ safe_previous_day, 48 строк без NaN для первого и последнего выпуска, офлайн с кэшем и без). Кэш `best_match` и `gfs_seamless` до 2026-03-02 (end_date сдвинут по [IDEA] Ансара: последний выпуск покрывает 01.03), `meta.json` с url/fetched_at/sha256 по моделям. Поля только day2 (dir/temp/gust) при lead ≥ 42 протягиваются с последнего безопасного часа.
