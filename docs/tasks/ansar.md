# ansar — задачи

<!-- Меняет только ansar (или его агент). Остальные читают: scripts/sync.sh показывает «Сейчас» и «Нужно от других» у всех. -->

Роль: **платформа** — API, страница, README, тесты API/CLI, smoke. Всё, что видит жюри. Стиль: красиво, чётко, минималистично, понятно; UI — по скиллу `ui-craft`, текст по-русски, без лишних экранов. Пуш через `scripts/checkpoint.sh` каждые ≤ 30 мин.

## Сейчас
- R6/R8: финальная интеграция README и checkpoint; полный smoke кандидата пройден (202 tests), UI проверен в Chrome. Новые числовые артефакты лид публикует отдельно, таблицы README синхронизируются только с опубликованными JSON.

## Дальше
- После 16:30 — только фиксы, README, скриншот 1280×800 в README, сдача на платформе вместе с Мустафой.

## Нужно от других
- mustafa: реальный коммит features/model/метрик после исправления переносимости; d70f38b содержит только PROGRESS. Сообщено в team-chat, временные числа не выдаём за опубликованные.

## Готово
- R8: request token после обоих HTTP-ответов и в обработчике ошибки; очистка старого выпуска при загрузке, блокировка экспорта; регрессия на переставленные ответы/ошибки.
- R8: статусы рассмотрения, предупреждение при недоступном журнале, таблица происхождения по часам, CSV всех ревизий, максимальный сдвиг ревизий. Ширина интервала в п.п., деньги обозначены сценарной оценкой.
- R6: smoke с проверкой хешей, обучением с нуля, всеми 28 выпусками и 672 часами в отдельном клоне, тестами и HTTP; логи окружения и результата. TESTING обновлён, сценарий демонстрации подготовлен.
- R8 шаг 1 — `app/api/routes.py`: 5 эндпоинтов по контракту на образце выпуска. `ruff` чисто, `pytest` 14/14, `curl /api/forecast/2026-01-31` → 48 строк с summary из `report.md` и warnings из лога, `POST /api/run` → 503 «агент ещё не подключён», 404 на неизвестный выпуск и прогон.
- R8 шаг 2 — `static/index.html`: график 48 ч (полоса p10–p90 окрашена по свежести прогноза day1/day2/day3 — правило утечки видно глазами), граница 24h|48h, переключатель парк/турбина 1/2, трасса агента с решениями, сводка, метрики. Без сети: инлайновый SVG вместо Chart.js с CDN. Скриншот снят в Chrome 1280×800.
