# mustafa — задачи

<!-- Меняет только mustafa (или его агент). Остальные читают: scripts/sync.sh показывает «Сейчас» и «Нужно от других» у всех. -->

## Сейчас
- 17:26 проверенный срез e3e7fb8: 203 tests, Ruff, web typecheck/lint/build; 29 CSV одинаковы на macOS, Windows, Linux Docker. Завершаем подписи integration и README, не добавляем новые функции в проверенный срез.
- 17:03 финальная интеграция: Codex проверяет Python/API, контракт и воспроизводимость; Claude владеет `web/`, Ансар — лёгким UI/README/TESTING. Рабочие копии раздельные, изменения согласуются через канал команды.
- Проверить собранную платформу из GitHub в браузере, завершить замечания владельцами, выполнить итоговый checkpoint до 17:40.

## Дальше
- R9: внешний API отложен Мустафой, `DEMO_MODE=1`; ключ остаётся только локально. Настоящий LLM-прогон пока не подтверждён.
- Капитан проверяет приём решения на платформе организатора. Аудит и готовые артефакты не означают автоматическую сдачу.

## Нужно от других
- Claude (`mustafa`, читать `scripts/msg.sh log`, одинаковая роль скрывает входящие): финальное ревью `web/` — различать refresh-сверку архива и будущий онлайн-прогноз; cron вынести в будущий пилот; health означает настройку LLM, не успешный API; интервалы/легенда только для станции; новые цифры взять из METRICS после фикса переносимости.
- amirkhan: финальная проверка Windows с новым pickle после canonical features; сравнить 29 CSV с GitHub. Старое расхождение точно воспроизведено как 1 ULP в dir_cos, не считать малой погрешностью.
- ansar: финальные README/TESTING/static/smoke; числа после фикса — Jan15.36%, Feb17.78%, CQR80.5%/82.0%. Полная валидация без внешнего LLM API.
- ansar, README (16:55): вставить после «Быстрый старт» раздел платформы:

  ### Платформа для диспетчера (Next.js, по желанию)
  Интерактивный веб-интерфейс поверх того же API: обзор февраля (тепловая карта по часам), выпуски с графиком 48 ч и пересчётом, суточная заявка в МВт·ч с выгрузкой CSV, журнал агента, точность и доказательства, «Спросить агента», раздел для интеграции. Без платформы решение работает полностью.
  ```bash
  uv run uvicorn app.main:app --port 8000   # терминал 1: API
  cd web && pnpm install && pnpm build && pnpm start   # терминал 2: http://localhost:3000
  ```
  Требования: Node.js 22+ и pnpm 11 (`corepack enable`). Адрес API задаёт `WINDCAST_API_URL` (по умолчанию http://localhost:8000); запросы `/api/*` проксируются, CORS не нужен.
- ansar, UI (ревью 15:10, страница на 1280×800 хорошая, правки по важности):
  1. Разрыв полосы p10–p90 на смене свежести погоды (1 февраля 17:00–18:00) — полоса должна быть непрерывной, свежесть показывать заливкой/штриховкой одной полосы.
  2. Пересчёт как главный «вау»: в CSV теперь ревизии 0 (48 ч) и 1 (часы 12–47, пересчёт в t0+12 ч). Показать rev0 пунктиром, rev1 сплошной линией, подпись «пересчитано в 12:00: вышел более свежий прогон».
  3. Шаги агента по-русски крупно («План», «Погода», «Проверка погоды», «Подготовка данных», «Модель», «Анализ», «Пересчёт», «Отчёт»), имя функции мелко рядом.
  4. Под графиком строка источника: «Погода: Open-Meteo Previous Runs (архив прогнозов), каждый час — из прогона, опубликованного до момента прогноза» + счётчик day1/day2/day3.
  5. Иконка сайта (inline SVG data-URI) — сейчас 404 в консоли.
  6. Сводка — простые абзацы/список, markdown-разметку не показывать.
- ansar, тесты: `tests/test_api.py` поправлен лидом под две ревизии (rev0 = 48 строк, rev1 = часы ≥ 12; warnings = шаги warn/fail из лога) — ZONE_OK, чтобы main был зелёным.
- amirkhan: `app.weather.load_or_fetch`, `select_for_issue` (R2), кэш с day3.
- ansar: `app/api/routes.py` на образце, страница.

## Готово
- 17:19 R3/R7: устранена разница macOS/Windows из-за одного ULP тригонометрии на пороге дерева; общая канонизация признаков 12 знаков, новые регрессионные проверки и полное переобучение/regen. Январь nMAE15.36%, февраль17.78%; CQR80.5/82.0%. Replay план MAE.1519 против GBM.1536; CI пересчёта включает0. Все5 сбоев дают48часов. 2296 строк устойчивы к ±1 ULP вычислений; нативная Windows перепроверяется командой после push.
- 17:03 R4/R6: частичный backtest сохраняет отдельный range-файл; обратный диапазон отвергается до расчёта; месячный CSV проверяется на полное покрытие и записывается атомарно. 11 новых проверок, всего 177 passed / ruff clean до интеграции web. Месячный CSV после нового writer побайтно прежний.
- 17:02 R8/R9: Claude опубликовал `web/` (Next.js, 7 экранов) и POST /api/ask, раскрыл подготовленный стартер в PREPARED. Идёт независимая проверка интерфейса.
- 16:57 R4/R6: чистый clone 0d36840, frozen install / ruff /155 tests прошли; backtest 55,86 с, все 29 CSV побайтно совпадают. Два POST /run: 82 строки, 9 шагов, одинаковый CSV, 5,61 и 1,95 с. Выпуск внутри обучения →422.
- 16:48 R1–R7: production model переобучен, метрики обоих периодов, 28 выпусков/672 часа февраля, replay и пять fault-сценариев пересчитаны. 155 тестов проходят. Исправлены fallback при исключениях и NaN, проверка GFS, отказ при недопустимой климатологии, сохранение rev0; nullable ветер проходит CSV→API. Публикация защищена блокировками и атомарной заменой файлов. Устаревшие 28 корневых runs перенесены в локальный backup вне репо; актуальные run_id согласованы с CSV. README/TESTING ждут финальных данных от Ансара.
- 16:34 Codex critics: устранены NaN в JSON супервизора, повторный учёт токенов, принятие finish до чтения инструментов, дубликаты tool-call ID; 21 проверка супервизора. Таймауты текстового LLM ограничены, метаданные провайдера изолированы по контексту запроса. REQUIREMENTS синхронизирован с R9 и настоящим источником p10/p90.
- 16:28 Codex: R9 `app/agent/planner.py` выбирает диагностические tools и направляет выпуск в accepted/review под обязательными проверками; CLI `--supervise`; `docs/AGENTIC.md`. R1/R3/R7: метка станции требует обеих турбин, отдельные кривые признаков квантилей и непересекающиеся целевые часы обучения/калибровки. Реальная проверка LLM ожидает локальную настройку ключа, не заявляется выполненной.
- 14:35 образец выпуска для платформы: `outputs/forecasts/issue_2026-01-31.csv`, `runs/20260201T0000-sample/`.
- kickoff: ТЗ, каркас py, зависимости (sklearn, lightgbm, pyarrow), данные и кэш погоды в репо, контракт `app/schemas.py` + `app/config.py`, зоны, задачи.
